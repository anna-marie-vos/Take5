<div class="row">
  <header class="pageTitle">
    <h1 class="Title">
      {{projectData.project_name}} Hazard Log
    </h1>
  </header>
  <div class="HazardLog">
    <h2>Hazard log</h2>
    <table>
      <tr> <th>ID:</th><th>Service:</th><th>Hazard:</th><th>Consequence:</th>
            <th>Expand:</th><th>Edit:</th></tr>
      {{#each allData}}
      <tr><td>{{hazard_id}}</td><td>{{discipline_name}}</td>
          <td>{{hazard}}</td><td>{{consequence}}</td>
          <td><input type="submit" name = "expand" value = "expand" /></td>
          <td><input type="submit" name = "edit" value = "edit" /></td>
      </tr>
      {{/each}}
      <input type="submit" name = "New" value = "New" />
    </table>
  </div>
</div>
